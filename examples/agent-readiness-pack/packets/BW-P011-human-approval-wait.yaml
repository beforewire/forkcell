schema_version: beforewire.packet.v1
id: BW-P011-human-approval-wait
title: Approval wait produces a resumable approval receipt
mode: shadow-readonly
surfaces: [approval, filesystem, package_manager, codex]
action_intent:
  actor: codex
  primitive: filesystem.write
  resource: filesystem:workspace:package.json
  right: write
  path: package.json
  diff_summary: "Add a lifecycle script that requires human approval before write."
expected:
  agt_effect: ask
  openshell_effect: ask
  risk_labels: [dependency_lifecycle, approval_required]
  side_effect_performed: false
  receipt_fields: [approval.request_id, approval.action_identity, approval.decision, approval.policy_hash]
